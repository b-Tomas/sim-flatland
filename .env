# Docker Compose default profiles.
# "agent" starts the InOrbit ROS2 agent + log tail alongside the simulation.
# To skip the agent (equivalent of --no-agent), override on the command line:
#     COMPOSE_PROFILES= docker compose up
COMPOSE_PROFILES=agent
